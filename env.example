# AI Usage Tracker - Environment Variables Example
# Copy this file to .env and fill in your values

# Django Server Configuration
# URL where the CLI sends data (your Django backend)
DJANGO_API_URL=https://api.pricepertoken.com

# Server JWT Secret (for CLI-to-server authentication)
# This should be a strong, random secret shared between CLI and Django
SERVER_JWT_SECRET=your-super-secret-jwt-key-here-change-this-in-production

# CLI Configuration
# User's personal API token (obtained from Django dashboard)
AI_USAGE_TRACKER_TOKEN=your-personal-api-token-from-dashboard

# Optional: Override Django URL for development/testing
# DJANGO_API_URL=http://localhost:8000

# Optional Redis Configuration (for Django rate limiting)
# REDIS_URL=redis://localhost:6379

# Development/Testing (optional)
# DEBUG=true
# LOG_LEVEL=DEBUG 